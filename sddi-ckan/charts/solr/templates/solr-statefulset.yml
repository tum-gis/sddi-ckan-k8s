{{- if .Values.enabled -}}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "solr.fullname" . }}-{{ .Values.component }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "solr.labels" . | nindent 4 }}
    app.kubernetes.io/part-of: sddi-ckan
    app.kubernetes.io/component: {{ .Values.component }}
spec:
  serviceName: {{ .Chart.Name }}-svc-headless
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "solr.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: {{ .Values.component }}
  template:
    metadata:
      labels:
        {{- include "solr.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: {{ .Values.component }}
    spec:
      securityContext:
        runAsUser: 8983
        runAsGroup: 8983
        fsGroup: 8983

      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: {{ include "solr.fullname" . }}-pvc
        - name: share
          emptyDir: {}

      initContainers:
        - name: get-jts
          image: bwibo/k8s-init-container
          imagePullPolicy: Always
          args:
            - 'wget -O /mydata/jts-core-1.19.0.jar
              "https://github.com/locationtech/jts/releases/download/1.19.0/jts-core-1.19.0.jar"
              && chown -v 0:0 /mydata/jts-core-1.19.0.jar'

          volumeMounts:
            - name: share
              mountPath: /mydata

          securityContext:
            runAsUser: 0
            runAsGroup: 0

      containers:
        - name: solr
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8983

          volumeMounts:
            - name: data
              mountPath: /var/solr/data
            - name: share
              mountPath: /opt/solr/server/solr-webapp/webapp/WEB-INF/lib/jts-core-1.19.0.jar
              subPath: jts-core-1.19.0.jar

          # livenessProbe:
          #   periodSeconds: 10
          #   timeoutSeconds: 2
          #   exec:
          #     command: [ sh, -c, "pg_isready -U $POSTGRES_USER -d $POSTGRES_DB" ]

          # readinessProbe:
          #   initialDelaySeconds: 15
          #   periodSeconds: 10
          #   timeoutSeconds: 2
          #   exec:
          #     command: [ sh, -c, "pg_isready -U $POSTGRES_USER -d $POSTGRES_DB" ]

          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

{{- end -}}
